<!-- copilot-tgrep-search:start -->
For broad or repeated text discovery, use tgrep-search with a ready index. Load the skill once, batch terms, request compact output. For explanations, include current context in the first search: those excerpts already count as current file reads. Read only missing ranges; stop when the requested evidence is sufficient. Use IDE tools for known files, semantic references and unsaved buffers. Preserve scope and freshness. If undiscovered, read %USERPROFILE%/.copilot/skills/tgrep-search/SKILL.md when available. Use the host's completion tool when required, then answer. Ordinary searches do not authorize installation or settings changes.
<!-- copilot-tgrep-search:end -->
