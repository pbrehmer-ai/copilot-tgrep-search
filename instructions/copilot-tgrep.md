<!-- copilot-tgrep-search:start -->
For broad or repeated repository text discovery, use the tgrep-search skill and reuse a ready index. Load the skill once per chat; use its compact count/path helper without rereading scripts or repeating schema/status discovery. Read known files directly and use IDE tools for semantic references or unsaved buffers. Preserve requested freshness and scope. If the skill is not discovered, read %USERPROFILE%/.copilot/skills/tgrep-search/SKILL.md if available; otherwise use existing search tools. Ordinary searches do not authorize installation or settings changes.
<!-- copilot-tgrep-search:end -->
